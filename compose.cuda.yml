# CUDA (NVIDIA GPU) overlay — use with:
#   docker compose -f compose.yml -f compose.cuda.yml up
services:
  server:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
